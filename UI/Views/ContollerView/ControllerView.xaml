<UserControl x:Class="UI.Views.ContollerView.ControllerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UI.Views.ContollerView"
             xmlns:controllerViewModel="clr-namespace:LJX8000.Core.ViewModels.ControllerViewModel;assembly=LJX8000.Core"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:UI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <StackPanel>
            <Button Command="{Binding ConnectCommand}">Connect</Button>
            <Button Command="{Binding InitHighSpeedCommand}">Init simple array</Button>
            <Button Command="{Binding PreStartHighSpeedCommand}">Pre-start</Button>
            <Button Command="{Binding StartHighSpeedCommand}">Start high speed</Button>
            <Button Command="{Binding StopHighSpeedCommand}">Stop high speed</Button>
            <Button Command="{Binding FinalizeHighSpeedCommand}">Finalize high speed</Button>
            <Button Command="{Binding SaveImageCommand}">Serialize</Button>
            <Button Command="{Binding DisconnectCommand}">Disconnect</Button>
            <GroupBox Header="Image Serialization" Margin="5 20">
                <StackPanel>
                    <Button Command="{Binding ResetCurrentImageIndexCommand}">Reset Index</Button>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock>Rows Per Image: </TextBlock>
                        <materialDesign:Badged Badge="{Binding CollectedRows}">
                            <TextBox Margin="5 0" Text="{Binding RowsPerImage}" />
                        </materialDesign:Badged>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock>Profile Count Each Fetch: </TextBlock>
                        <TextBox Margin="5 0" Text="{Binding ProfileCountEachFetch}" />
                    </StackPanel>
                    <TextBox materialDesign:HintAssist.Hint="Image Directory" Text="{Binding SerializationDirectory}" />
                </StackPanel>
            </GroupBox>

            <Border>
                <materialDesign:PackIcon Kind="{Binding Status, Converter={converters:StatusToIconConverter}}" />
            </Border>
        </StackPanel>


    </Grid>

</UserControl>